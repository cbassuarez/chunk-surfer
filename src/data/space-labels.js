// Player-facing place names live here; floorplan, save and prop identifiers do
// not follow display copy. In particular, `get-in`, `dock-*` and dockHaunting
// are persisted wire names and remain stable for old runs.

export const SCENE_DOCK_LABEL = 'SCENE DOCK';
export const SCENE_DOCK_NAME = 'Scene Dock';
