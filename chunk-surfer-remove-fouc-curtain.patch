diff --git a/index.html b/index.html
index a71e3e6..486c37d 100644
--- a/index.html
+++ b/index.html
@@ -1,5 +1,5 @@
 <!doctype html>
-<html lang="en" class="cs-critical">
+<html lang="en">
 <head>
   <meta charset="utf-8">
   <meta name="viewport" content="width=device-width, initial-scale=1">
@@ -9,11 +9,10 @@
     :root {
       color-scheme: dark;
       background: #020807;
-      --boot-bg: #020807;
-      --boot-glass: rgba(0, 12, 10, 0.82);
-      --boot-cyan: #79ffe8;
-      --boot-cyan-dim: rgba(121, 255, 232, 0.48);
-      --boot-amber: #ffb23f;
+      --critical-bg: #020807;
+      --critical-fg: #dce8e5;
+      --critical-muted: rgba(220, 232, 229, 0.66);
+      --critical-rule: rgba(220, 232, 229, 0.34);
     }
 
     html,
@@ -22,89 +21,89 @@
       width: 100%;
       min-width: 100%;
       min-height: 100%;
-      background:
-        radial-gradient(circle at 50% 44%, rgba(42, 255, 225, 0.08), transparent 42%),
-        linear-gradient(180deg, #020807 0%, #000302 100%);
-      color: var(--boot-cyan);
+      background: var(--critical-bg);
+      color: var(--critical-fg);
       overflow: hidden;
     }
 
-    body.cs-preboot {
-      cursor: wait;
+    body {
+      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
     }
 
-    body.cs-preboot * {
-      outline: none !important;
+    #wrap {
+      position: fixed;
+      inset: 0;
+      overflow: hidden;
+      background: var(--critical-bg);
     }
 
-    body.cs-preboot #wrap {
-      opacity: 0;
-      visibility: hidden;
-      pointer-events: none;
+    #map {
+      position: absolute;
+      inset: 0;
+      white-space: pre;
+      font: inherit;
     }
 
-    body.cs-ready #wrap {
+    #introTitle {
+      position: absolute;
+      left: 50%;
+      top: 34%;
+      transform: translate(-50%, calc(-50% + var(--introLift, 0px)));
+      z-index: 2;
+      text-align: center;
+      letter-spacing: 0.22em;
+      color: var(--critical-fg);
+      text-shadow: 0 0 18px rgba(220, 232, 229, 0.18);
+      user-select: none;
+      pointer-events: none;
       opacity: 1;
-      visibility: visible;
     }
 
-    .boot-curtain {
-      position: fixed;
-      inset: 0;
-      z-index: 2147483647;
-      display: grid;
-      place-items: center;
-      background:
-        repeating-linear-gradient(
-          0deg,
-          rgba(121, 255, 232, 0.035) 0,
-          rgba(121, 255, 232, 0.035) 1px,
-          transparent 1px,
-          transparent 7px
-        ),
-        radial-gradient(circle at 50% 42%, rgba(121, 255, 232, 0.10), transparent 40%),
-        #020807;
-      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
-      letter-spacing: 0.08em;
-      color: var(--boot-cyan);
-      transition: opacity 180ms ease;
+    #introTitleAuthor {
+      margin-bottom: 38px;
+      color: var(--critical-muted);
+      font-size: 13px;
+      letter-spacing: 0.42em;
+      text-transform: lowercase;
     }
 
-    body.cs-ready .boot-curtain {
-      opacity: 0;
-      pointer-events: none;
+    #introTitleWork {
+      font-size: clamp(24px, 3.1vw, 44px);
+      font-weight: 700;
+      letter-spacing: 0.44em;
+      white-space: nowrap;
     }
 
-    .boot-curtain__panel {
-      min-width: min(360px, calc(100vw - 48px));
-      border: 1px solid rgba(121, 255, 232, 0.32);
-      background: var(--boot-glass);
-      box-shadow: 0 0 32px rgba(121, 255, 232, 0.10);
-      padding: 28px 34px;
-      text-align: left;
+    #introTitleRule {
+      width: min(260px, 42vw);
+      height: 1px;
+      margin: 18px auto 14px;
+      background: var(--critical-rule);
     }
 
-    .boot-curtain__kicker {
-      opacity: 0.65;
-      font-size: 12px;
-      margin-bottom: 10px;
+    #introTitleDesc {
+      font-size: 15px;
+      letter-spacing: 0.08em;
+      color: var(--critical-fg);
     }
 
-    .boot-curtain__title {
-      font-size: 20px;
-      text-shadow: 0 0 12px currentColor;
+    #introTitleSub {
+      margin-top: 28px;
+      color: var(--critical-muted);
+      font-size: 13px;
+      letter-spacing: 0.30em;
     }
 
-    .boot-curtain__status {
-      margin-top: 16px;
-      color: var(--boot-amber);
-      font-size: 12px;
-      opacity: 0.85;
+    #introVignette,
+    #hushJump {
+      position: absolute;
+      inset: 0;
+      pointer-events: none;
     }
   </style>
   <link rel="stylesheet" href="./styles.css?v=20260710vfd2">
 </head>
-<body class="cs-preboot">
+<body>
 <div id="wrap">
   <div id="introTitle">
     <div id="introTitleAuthor">cbassuarez</div>
@@ -117,13 +116,6 @@
   <div id="introVignette"></div>
   <div id="hushJump"></div>
 </div>
-<div class="boot-curtain" aria-hidden="true">
-  <div class="boot-curtain__panel">
-    <div class="boot-curtain__kicker">AUDIOCORP FIELD MONITOR</div>
-    <div class="boot-curtain__title">CHUNK SURFER</div>
-    <div class="boot-curtain__status">INITIALIZING DISPLAY BUS…</div>
-  </div>
-</div>
 
 <script type="module" src="./src/main.js?v=20260710vfd2"></script>
 </body>
diff --git a/package.json b/package.json
index 4aae86f..202fa3f 100644
--- a/package.json
+++ b/package.json
@@ -15,7 +15,7 @@
     "tauri:build": "tauri build",
     "test:desktop-menu": "node --test test/desktop-menu-actions.test.mjs test/desktop-menu-bridge.test.mjs test/desktop-global-shortcuts.test.mjs test/game-mode.test.mjs test/desktop-menu-contract.test.mjs",
     "tauri:check": "cargo check --manifest-path src-tauri/Cargo.toml --all-targets",
-    "test:display": "node --test test/display-policy.test.mjs test/display-contract.test.mjs test/viewport-guard.test.mjs test/settings-vs-pause.test.mjs test/desktop-window-contract.test.mjs test/about-system.test.mjs test/performance-meter.test.mjs test/settings-about-tab.test.mjs test/credits-data.test.mjs test/boot-curtain.test.mjs",
+    "test:display": "node --test test/display-policy.test.mjs test/display-contract.test.mjs test/viewport-guard.test.mjs test/settings-vs-pause.test.mjs test/desktop-window-contract.test.mjs test/about-system.test.mjs test/performance-meter.test.mjs test/settings-about-tab.test.mjs test/credits-data.test.mjs test/critical-title-paint.test.mjs",
     "test:desktop": "npm run test:desktop-menu && npm run test:display"
   },
   "devDependencies": {
diff --git a/src/main.js b/src/main.js
index 55abfa8..6f6f289 100644
--- a/src/main.js
+++ b/src/main.js
@@ -63,7 +63,6 @@ import { resolveDesktopPaths } from './platform/paths/desktopPaths.js';
 import { revealPath } from './platform/diagnostics/desktopDiagnostics.js';
 import { runtimeParams, runtimeSnapshot } from './platform/launch.js';
 import { APP_COPYRIGHT, APP_LINKS, copyText, formatDiagnosticReport, formatLens, normalizeAboutSnapshot } from './platform/about-system.js';
-import { markAppReady } from './platform/app-ready.js';
 import { createPerformanceMeter } from './platform/performance-meter.js';
 import * as STAB from './game/stabs.js';
 import * as OBJ from './game/objectives.js';
@@ -8516,7 +8515,6 @@ async function boot(){
   updateOnboardingButton();
     await bootScenes();
     raf=requestAnimationFrame(loop);
-    markAppReady();
     const qp=params();
     if(!qp.has('baglab')&&!qp.has('progresslab')&&!qp.has('maplab')&&!qp.has('hushaudiolab')){
       loadAll();
diff --git a/src/platform/app-ready.js b/src/platform/app-ready.js
deleted file mode 100644
index f7b0cdb..0000000
--- a/src/platform/app-ready.js
+++ /dev/null
@@ -1,26 +0,0 @@
-let readyMarked = false;
-
-function afterNextPaint(callback) {
-  const raf = globalThis.requestAnimationFrame || ((fn) => setTimeout(fn, 16));
-  raf(() => raf(callback));
-}
-
-export function markAppReady({ removeDelayMs = 260, document: doc = globalThis.document } = {}) {
-  if (readyMarked || !doc?.body) return false;
-  readyMarked = true;
-
-  afterNextPaint(() => {
-    doc.body.classList.remove('cs-preboot');
-    doc.body.classList.add('cs-ready');
-
-    globalThis.setTimeout?.(() => {
-      doc.querySelector?.('.boot-curtain')?.remove?.();
-    }, removeDelayMs);
-  });
-
-  return true;
-}
-
-export function resetAppReadyForTests() {
-  readyMarked = false;
-}
diff --git a/test/boot-curtain.test.mjs b/test/boot-curtain.test.mjs
deleted file mode 100644
index 2d3a9a6..0000000
--- a/test/boot-curtain.test.mjs
+++ /dev/null
@@ -1,17 +0,0 @@
-import test from 'node:test';
-import assert from 'node:assert/strict';
-import { readFileSync } from 'node:fs';
-
-test('index owns first paint with static preboot curtain', () => {
-  const html = readFileSync('index.html', 'utf8');
-  assert.match(html, /<body class="cs-preboot">/);
-  assert.match(html, /body\.cs-preboot #wrap/);
-  assert.match(html, /boot-curtain/);
-  assert.match(html, /INITIALIZING DISPLAY BUS/);
-  assert.match(html, /#020807/);
-});
-
-test('preboot does not wait for DOMContentLoaded', () => {
-  const html = readFileSync('index.html', 'utf8');
-  assert.doesNotMatch(html, /DOMContentLoaded[^<]+cs-preboot/s);
-});
diff --git a/test/critical-title-paint.test.mjs b/test/critical-title-paint.test.mjs
new file mode 100644
index 0000000..43a87db
--- /dev/null
+++ b/test/critical-title-paint.test.mjs
@@ -0,0 +1,14 @@
+import test from 'node:test';
+import assert from 'node:assert/strict';
+import { readFileSync } from 'node:fs';
+
+test('index uses immediate critical title styling without a boot curtain', () => {
+  const html = readFileSync('index.html', 'utf8');
+  assert.match(html, /<meta name="color-scheme" content="dark">/);
+  assert.match(html, /#introTitle\s*{/);
+  assert.match(html, /#introTitleWork\s*{/);
+  assert.match(html, /#020807/);
+  assert.doesNotMatch(html, /cs-preboot/);
+  assert.doesNotMatch(html, /boot-curtain/);
+  assert.doesNotMatch(html, /INITIALIZING DISPLAY BUS/);
+});
