# The local half. `python -m venv .venv-local && .venv-local/bin/pip install -r
# requirements-local.txt`, then `.venv-local/bin/python server.py`.
#
# The local runtime is the production inference path. peft is not optional:
# load_lora_weights(adapter_name=...) and set_adapters() are peft.
#
# torch and torchvision are DELIBERATELY not listed here. `pip install torch`
# from the default index is a CPU-only wheel on Windows, which is what shipped a
# GPU-less sidecar to every Windows player. Install them first with
# `python install_torch.py`, which picks the cu128 index (Windows/Linux) or the
# MPS build (macOS). diffusers/transformers/etc. then install against whatever
# torch is already present.
diffusers==0.31.0
transformers==4.46.3
huggingface_hub==0.26.5
accelerate==1.1.1
peft==0.13.2
# Chunked, weighted prompt embeddings. Without it CLIP truncates at 77 tokens
# and silently drops the tail of every prompt.
compel==2.0.3
safetensors
pillow
fastapi
uvicorn[standard]
websockets
